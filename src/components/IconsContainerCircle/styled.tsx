import { rem } from "@/helpers/rem";
import styled from "styled-components";

export const IconsContainerCircle = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  width: ${rem(38)};
  height: ${rem(38)};
  border-radius: 50%;
  background: #19202e;
`;
